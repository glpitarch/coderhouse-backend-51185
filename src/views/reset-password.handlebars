<div class="r-flex justify-content-center mt-5">
    <div class="c-flex align-items-center txt-center min-width-25 border-form pb-2">
        <div class="my-1">
            <h1>Restablecer contraseña</h1>
            <p>Ingresa tu nueva contraseña</p>
            <form method="post" action="/api/session/reset-password?token={{token}}">
                <div class="c-flex align-items-center">
                    <div class="c-flex">
                        <label class="mb-02e5">Dirección de e-mail</label>
                        <input type="email" class="txt-center" placeholder="ejemplo@servidor.com" name="email"/>
                        <label class="mt-1 mb-02e5">Nueva contraseña</label>
                        <input class="txt-center mb-1" type="password" placeholder="nueva contraseña" name="newPassword"/>
                    </div>
                    <button type="submit">Restablecer contraseña</button>
                </div>
            </form>
        </div>
        <p>Para volver a la pantalla de login haz click <a href="/">aquí</a></p>
    </div>
</div>
